#pragma once

enum class ZoneType {
    Residential,
    Industrial,
    Service,
    None,

    Count // Cool trick to get the count of the enum values.
};        // They must not be specified explicitly.

